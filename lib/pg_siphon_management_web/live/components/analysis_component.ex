defmodule PgSiphonManagementWeb.AnalysisComponent do
  use PgSiphonManagementWeb, :live_component

  def mount(socket) do
    {:ok, socket}
  end

  def render(assigns) do
    ~H"""
    """
  end
end
